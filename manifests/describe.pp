#
#+ @name: postgresql-server 
#+ @type: package
#+ @desc: Install postgresql server package and manages it on the host. Package will be _reinstalled_ if removed.
#+
#+
#+ @name: /etc/sysconfig/pgsql/postgresql
#+ @type: file
#+ @desc: RedHat-based systems hardcode some PG* variables in the init script.
#
